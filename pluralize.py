def pluralize(word): { # 1. word is passed as input PARAMETER into the function...
    return word + "s" # ...word is RETURNED  in place at the function call with processed data.


userword = input("Word please:")
print(pluralize(userword)); # userword is a variable that's passed as an AGRUMENT to the function call.
